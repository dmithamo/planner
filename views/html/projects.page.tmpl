{{template "base" .}}
{{define "title"}}projects{{end}}
{{define "body"}}
<div class="container">
  <div class="container-nav">
    <p class="nav-header">
      <span>Projects</span>
      <a class="cta create-new" href="/projects/create">New &rarr;</a>
    </p>
    {{with .}}
    <ul class="nav-list">
    {{range .}}
    <li><a href="/projects/{{.ProjectID}}">{{.Title}}</a></li>
    {{end}}
    </ul>
    {{else}}
    0 Projects
    {{end}}
  </div>
  <div class="container-main">There will be stuff here (useful stuff)</div>
</div>
{{end}}
